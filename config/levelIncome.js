/** MLM level income & genealogy depth — L1 through L11 only. */
const LEVEL_PERCENTS = [5, 3, 2, 1, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5];

const MAX_NETWORK_LEVELS = LEVEL_PERCENTS.length;

module.exports = { LEVEL_PERCENTS, MAX_NETWORK_LEVELS };
